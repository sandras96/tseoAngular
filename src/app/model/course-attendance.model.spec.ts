import { CourseAttendance } from './course-attendance.model';

describe('CourseAttendance', () => {
  it('should create an instance', () => {
    expect(new CourseAttendance()).toBeTruthy();
  });
});
